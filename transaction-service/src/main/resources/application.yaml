spring:
    application:
        name: transaction-service
    config:
        import: "optional:configserver:http://localhost:8088"

management:
    endpoints:
        web:
            exposure:
                include: "*"
    endpoint:
        health:
            show-details: always
    info:
        env:
            enabled: true


